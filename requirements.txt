#################################################################
# VulnEagle Dependencies (Kali Optimized)
#
# Kali / Debian users SHOULD prefer system packages:
#   sudo apt install -y python3-requests python3-dnspython python3-yaml
# and then run the tool directly (no virtualenv needed).
#
# This file is only for pip fallback or non-Debian systems.
# Versions are relaxed to avoid conflicts with apt-provided builds.
# If you want fully reproducible builds, create a separate lock file.
#################################################################

# Core (required)
requests>=2.28
dnspython>=2.4

# Optional (YAML config support). Safe to remove if you export env vars instead.
pyyaml>=6.0

# ---- Optional Enhancements (commented) ----
# rich>=13.0.0        # nicer progress / coloring (not required)
# colorama>=0.4.6     # Windows color support

# ---- Dev / QA (do not install in production) ----
# mypy>=1.8.0
# pytest>=7.4.0
# ruff>=0.2.0

# To generate a lock file (optional):
#   pip install pip-tools && pip-compile --resolver=backtracking -o requirements-lock.txt

# NOTE: If running on Kali and you already installed apt packages, you *can* skip
#   pip install -r requirements.txt
# Unless you need the optional extras above.